# TODO

- [x] Define the list of lexical functions (LFs) in a dedicated shared file.
- [ ] Add configuration files for core grammar constants (POS tags, variables, etc.).
- [ ] Add multilingual support for user-facing strings (i18n).
